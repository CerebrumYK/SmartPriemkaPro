/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'Cache-Control',
            value: 'no-cache, no-store, must-revalidate',
          },
        ],
      },
    ];
  },
  // Configure dev server to accept connections from any host (required for Replit proxy)
  devIndicators: {
    buildActivity: true,
  },
};

export default nextConfig;
